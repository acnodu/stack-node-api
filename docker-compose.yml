services:
    testage:
        container_name: api
        build:
            dockerfile: Dockerfile-compose
            context: https://github.com/acnodu/test-portainer
            ssh:
                - default
        stdin_open: true
        ports:
            - "8001:8001"
